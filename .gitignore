node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.*.example
!.env.production
.DS_Store
*.log
npm-debug.log*
.vite

# local build/test artefacts
tsconfig.tsbuildinfo
uitest.png

# Screenshots the Claude desktop app saves alongside the repo
Claude outputs/

*storybook.log
storybook-static

# Test screenshots land in the repo root; the owner's logo stays out of the
# deploy (public/icons/ carries what the site needs).
/*.png
public/branding/
